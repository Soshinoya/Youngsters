mixin buttonThird(text, id, btnType)
    button.button.button-third(id=(id ? id : '') type=(btnType ? btnType : ''))
        svg
            use(xlink:href="./images/sprite.svg#logo-white")
        p= text